# Copy to .env (gitignored). Adjust as needed.

NODE_RED_PORT=1880
MQTT_PORT=1883
TZ=Europe/London

# Used by Node-RED to encrypt credentials in flows.json — set this BEFORE
# you create any encrypted credential nodes; changing it later loses them.
NODE_RED_CREDENTIAL_SECRET=change-me-locally
